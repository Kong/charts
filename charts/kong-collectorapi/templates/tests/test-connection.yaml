apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kong-collectorapi.fullname" . }}-test-connection"
  labels:
{{ include "kong-collectorapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kong-collectorapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
