# This file is auto-generated by KO's hack/generators/conversion-webhook/main.go generator.
{{- if .Values.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
{{ if .Values.keep }}
    helm.sh/resource-policy: keep
{{ end }}
    kubernetes-configuration.konghq.com/channels: kong-operator
    kubernetes-configuration.konghq.com/version: v2.2.0
  name: eventgatewayvirtualclusterpolicies.configuration.konghq.com
spec:
  group: configuration.konghq.com
  names:
    categories:
    - kong
    kind: EventGatewayVirtualClusterPolicy
    listKind: EventGatewayVirtualClusterPolicyList
    plural: eventgatewayvirtualclusterpolicies
    singular: eventgatewayvirtualclusterpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Konnect ID
      jsonPath: .status.id
      name: ID
      type: string
    - description: The Resource is Programmed on Konnect
      jsonPath: .status.conditions[?(@.type=='Programmed')].status
      name: Programmed
      type: string
    - description: Konnect Organization ID this resource belongs to.
      jsonPath: .status.organizationID
      name: OrgID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EventGatewayVirtualClusterPolicy is the Schema for the eventgatewayvirtualclusterpolicys
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventGatewayVirtualClusterPolicySpec defines the desired
              state of EventGatewayVirtualClusterPolicy.
            properties:
              apiSpec:
                description: APISpec defines the desired state of the resource's API
                  spec fields.
                properties:
                  acls:
                    description: EventGatewayACLsPolicy configuration.
                    properties:
                      condition:
                        description: |-
                          A string containing the boolean expression that determines whether the
                          policy is applied.
                        maxLength: 1000
                        type: string
                      config:
                        description: The configuration of the policy.
                        properties:
                          rules:
                            description: Every ACL rule in this list applies independently.
                            items:
                              description: EventGatewayACLRule A Kafka ACL rule to
                                apply to virtual cluster traffic
                              properties:
                                action:
                                  description: How to handle the request if the rule
                                    matches
                                  enum:
                                  - allow
                                  - deny
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                operations:
                                  description: |-
                                    Types of Kafka operations to match against.
                                    Note that not every operation can apply to every resource type.
                                  items:
                                    description: |-
                                      EventGatewayACLOperation An Event Gateway operation to match against in an
                                      ACL rule.
                                    properties:
                                      name:
                                        enum:
                                        - all
                                        - alter
                                        - alter_configs
                                        - create
                                        - delete
                                        - describe
                                        - describe_configs
                                        - idempotent_write
                                        - read
                                        - write
                                        maxLength: 253
                                        minLength: 1
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  type: array
                                resourceNames:
                                  description: |-
                                    If any of these entries match, the resource name matches for this rule.
                                    A maximum of 50 entries are allowed.
                                  properties:
                                    dynam:
                                      description: Dynam configuration.
                                      type: string
                                    stat:
                                      description: Stat configuration.
                                      items:
                                        description: |-
                                          EventGatewayACLResourceName An Event Gateway resource name to match against
                                          in an ACL rule.
                                        properties:
                                          match:
                                            description: |-
                                              Currently supported are exact matches and globs.
                                              All `*` characters are interpreted as globs, i.e.
                                              they match zero or more of any character.
                                            maxLength: 253
                                            minLength: 1
                                            type: string
                                        required:
                                        - match
                                        type: object
                                      type: array
                                    type:
                                      description: Type designates the type of configuration.
                                      enum:
                                      - stat
                                      - dynam
                                      minLength: 1
                                      type: string
                                  required:
                                  - type
                                  type: object
                                resourceType:
                                  description: This rule applies to access only for
                                    type of resource
                                  enum:
                                  - topic
                                  - group
                                  - transactional_id
                                  - cluster
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                              required:
                              - action
                              - operations
                              - resourceNames
                              - resourceType
                              type: object
                            type: array
                        required:
                        - rules
                        type: object
                      description:
                        description: A human-readable description of the policy.
                        maxLength: 512
                        type: string
                      enabled:
                        description: Whether the policy is enabled.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      labels:
                        additionalProperties:
                          description: LabelsValue is the value type for Labels.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                          type: string
                        description: |-
                          Labels store metadata of an entity that can be used for filtering an entity
                          list or for searching across entity types.

                          Keys must be of length 1-63 characters, and cannot start with "kong",
                          "konnect", "mesh", "kic", or "_".
                        type: object
                      name:
                        description: A unique user-defined name of the policy.
                        maxLength: 255
                        type: string
                    required:
                    - config
                    type: object
                  type:
                    description: Type designates the type of configuration.
                    enum:
                    - acls
                    minLength: 1
                    type: string
                required:
                - type
                type: object
              eventGatewayVirtualClusterRef:
                description: EventGatewayVirtualClusterRef is the reference to the
                  parent EventGatewayVirtualCluster object.
                properties:
                  konnectID:
                    description: |-
                      KonnectID is the schema for the KonnectID type.
                      This field is required when the Type is konnectID.
                    maxLength: 36
                    type: string
                  namespacedRef:
                    description: |-
                      NamespacedRef is a reference to a KeySet entity inside the cluster.
                      This field is required when the Type is namespacedRef.
                    properties:
                      name:
                        description: Name is the name of the referred resource.
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referred resource.

                          For namespace-scoped resources if no Namespace is provided then the
                          namespace of the parent object MUST be used.

                          This field MUST not be set when referring to cluster-scoped resources.
                        maxLength: 253
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    description: |-
                      Type defines type of the object which is referenced. It can be one of:

                      - konnectID
                      - namespacedRef
                    enum:
                    - konnectID
                    - namespacedRef
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: when type is namespacedRef, namespacedRef must be set
                  rule: 'self.type == ''namespacedRef'' ? has(self.namespacedRef)
                    : true'
                - message: when type is namespacedRef, konnectID must not be set
                  rule: 'self.type == ''namespacedRef'' ? !has(self.konnectID) : true'
                - message: when type is konnectID, konnectID must be set
                  rule: 'self.type == ''konnectID'' ? has(self.konnectID) : true'
                - message: when type is konnectID, namespacedRef must not be set
                  rule: 'self.type == ''konnectID'' ? !has(self.namespacedRef) : true'
            required:
            - eventGatewayVirtualClusterRef
            type: object
          status:
            description: EventGatewayVirtualClusterPolicyStatus defines the observed
              state of EventGatewayVirtualClusterPolicy.
            properties:
              conditions:
                description: Conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              gatewayID:
                description: GatewayID is the Konnect ID of the parent Gateway.
                properties:
                  id:
                    description: ID is the unique identifier of the Konnect entity
                      as assigned by Konnect API.
                    maxLength: 256
                    type: string
                type: object
              id:
                description: |-
                  ID is the unique identifier of the Konnect entity as assigned by Konnect API.
                  If it's unset (empty string), it means the Konnect entity hasn't been created yet.
                maxLength: 256
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              organizationID:
                description: OrgID is ID of Konnect Org that this entity has been
                  created in.
                maxLength: 256
                type: string
              serverURL:
                description: ServerURL is the URL of the Konnect server in which the
                  entity exists.
                maxLength: 512
                type: string
              virtualClusterID:
                description: VirtualClusterID is the Konnect ID of the parent VirtualCluster.
                properties:
                  id:
                    description: ID is the unique identifier of the Konnect entity
                      as assigned by Konnect API.
                    maxLength: 256
                    type: string
                type: object
            type: object
        required:
        - metadata
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
