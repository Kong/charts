# This file is auto-generated by KO's hack/generators/conversion-webhook/main.go generator.
{{- if .Values.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
{{ if .Values.keep }}
    helm.sh/resource-policy: keep
{{ end }}
    kubernetes-configuration.konghq.com/channels: kong-operator
    kubernetes-configuration.konghq.com/version: v2.4.0-rapid.1
  name: konnectconfigstoresyncs.konnect.konghq.com
spec:
  group: konnect.konghq.com
  names:
    categories:
    - kong
    - konnect
    kind: KonnectConfigStoreSync
    listKind: KonnectConfigStoreSyncList
    plural: konnectconfigstoresyncs
    singular: konnectconfigstoresync
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Name of the referenced KonnectConfigStore
      jsonPath: .spec.configStoreRef.name
      name: Store
      type: string
    - description: Name of the referenced Secret
      jsonPath: .spec.secretRef.name
      name: Secret
      type: string
    - description: Sync mode (Combined or Split)
      jsonPath: .spec.mode
      name: Mode
      type: string
    - description: Number of entries currently synced to the Config Store
      jsonPath: .status.entriesSynced
      name: Entries
      type: integer
    - description: The entries are synced to the Config Store
      jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: Synced
      type: string
    - description: Age
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              combined:
                properties:
                  certificateField:
                    default: tls.crt
                    maxLength: 253
                    minLength: 1
                    pattern: ^[-._a-zA-Z0-9]+$
                    type: string
                  keyField:
                    default: tls.key
                    maxLength: 253
                    minLength: 1
                    pattern: ^[-._a-zA-Z0-9]+$
                    type: string
                  storeKey:
                    maxLength: 512
                    minLength: 1
                    pattern: ^[A-Za-z0-9._-]+$
                    type: string
                type: object
                x-kubernetes-validations:
                - message: spec.combined.certificateField and spec.combined.keyField
                    must be different
                  rule: self.certificateField != self.keyField
                - message: spec.combined.storeKey is immutable once set
                  rule: '!has(oldSelf.storeKey) || (has(self.storeKey) && self.storeKey
                    == oldSelf.storeKey)'
              configStoreRef:
                properties:
                  name:
                    maxLength: 253
                    minLength: 1
                    type: string
                  namespace:
                    maxLength: 253
                    type: string
                required:
                - name
                type: object
              deletionPolicy:
                default: Orphan
                enum:
                - Orphan
                - Delete
                type: string
              mode:
                default: Combined
                enum:
                - Combined
                - Split
                type: string
              secretRef:
                properties:
                  name:
                    maxLength: 253
                    minLength: 1
                    type: string
                  namespace:
                    maxLength: 253
                    type: string
                required:
                - name
                type: object
              split:
                properties:
                  entries:
                    items:
                      properties:
                        field:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[-._a-zA-Z0-9]+$
                          type: string
                        storeKey:
                          maxLength: 512
                          minLength: 1
                          pattern: ^[A-Za-z0-9._-]+$
                          type: string
                      required:
                      - field
                      type: object
                    maxItems: 64
                    minItems: 1
                    type: array
                    x-kubernetes-list-map-keys:
                    - field
                    x-kubernetes-list-type: map
                required:
                - entries
                type: object
                x-kubernetes-validations:
                - message: spec.split.entries storeKeys must be unique
                  rule: self.entries.all(e, self.entries.filter(o, has(o.storeKey)
                    && has(e.storeKey) && o.storeKey == e.storeKey).size() <= 1)
                - message: spec.split.entries storeKey is immutable for entries that
                    keep their field
                  rule: self.entries.all(n, !oldSelf.entries.exists(o, o.field ==
                    n.field && has(o.storeKey)) || (has(n.storeKey) && oldSelf.entries.exists(o,
                    o.field == n.field && has(o.storeKey) && o.storeKey == n.storeKey)))
            required:
            - configStoreRef
            - secretRef
            type: object
            x-kubernetes-validations:
            - message: exactly one of spec.combined or spec.split must be set, matching
                spec.mode
              rule: has(self.combined) == (self.mode == 'Combined') && has(self.split)
                == (self.mode == 'Split')
          status:
            properties:
              conditions:
                default:
                - lastTransitionTime: "1970-01-01T00:00:00Z"
                  message: Waiting for controller
                  reason: Pending
                  status: Unknown
                  type: ConfigStoreRefValid
                - lastTransitionTime: "1970-01-01T00:00:00Z"
                  message: Waiting for controller
                  reason: Pending
                  status: Unknown
                  type: SecretRefValid
                - lastTransitionTime: "1970-01-01T00:00:00Z"
                  message: Waiting for controller
                  reason: Pending
                  status: Unknown
                  type: PairValid
                - lastTransitionTime: "1970-01-01T00:00:00Z"
                  message: Waiting for controller
                  reason: Pending
                  status: Unknown
                  type: Synced
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                minItems: 4
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              controlPlaneID:
                maxLength: 256
                type: string
              entries:
                items:
                  properties:
                    hash:
                      maxLength: 71
                      pattern: ^sha256:[0-9a-f]{64}$
                      type: string
                    keyBytes:
                      format: int64
                      type: integer
                    lastPushTime:
                      format: date-time
                      type: string
                    notAfter:
                      format: date-time
                      type: string
                    observedUpdatedAt:
                      format: date-time
                      type: string
                    sourceFields:
                      items:
                        type: string
                      maxItems: 64
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                    storeKey:
                      maxLength: 512
                      minLength: 1
                      type: string
                    valueBytes:
                      format: int64
                      type: integer
                  required:
                  - sourceFields
                  - storeKey
                  type: object
                maxItems: 64
                type: array
                x-kubernetes-list-map-keys:
                - storeKey
                x-kubernetes-list-type: map
              entriesSynced:
                format: int32
                type: integer
              entriesTotal:
                format: int32
                type: integer
              observedSecretResourceVersion:
                maxLength: 64
                type: string
              references:
                items:
                  properties:
                    subfield:
                      maxLength: 64
                      minLength: 1
                      type: string
                    suffix:
                      maxLength: 577
                      minLength: 1
                      type: string
                  required:
                  - suffix
                  type: object
                maxItems: 64
                type: array
                x-kubernetes-list-map-keys:
                - suffix
                x-kubernetes-list-type: map
              storeID:
                maxLength: 256
                type: string
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: spec.mode is immutable
          rule: self.spec.mode == oldSelf.spec.mode
        - message: spec.configStoreRef.name is immutable
          rule: self.spec.configStoreRef.name == oldSelf.spec.configStoreRef.name
    served: true
    storage: true
    subresources:
      status: {}

{{- end }}
