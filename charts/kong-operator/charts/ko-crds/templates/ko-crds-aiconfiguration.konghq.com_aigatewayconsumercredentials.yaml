# This file is auto-generated by KO's hack/generators/conversion-webhook/main.go generator.
{{- if .Values.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
{{ if .Values.keep }}
    helm.sh/resource-policy: keep
{{ end }}
    kubernetes-configuration.konghq.com/channels: kong-operator
    kubernetes-configuration.konghq.com/version: v2.4.0-rapid.1
  name: aigatewayconsumercredentials.aiconfiguration.konghq.com
spec:
  group: aiconfiguration.konghq.com
  names:
    categories:
    - kong
    kind: AIGatewayConsumerCredential
    listKind: AIGatewayConsumerCredentialList
    plural: aigatewayconsumercredentials
    singular: aigatewayconsumercredential
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Konnect ID
      jsonPath: .status.id
      name: ID
      type: string
    - description: The Resource is Programmed on Konnect
      jsonPath: .status.conditions[?(@.type=='Programmed')].status
      name: Programmed
      type: string
    - description: Konnect Organization ID this resource belongs to.
      jsonPath: .status.organizationID
      name: OrgID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              aiGatewayConsumerRef:
                properties:
                  konnectID:
                    maxLength: 36
                    type: string
                  namespacedRef:
                    properties:
                      name:
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        maxLength: 253
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    enum:
                    - konnectID
                    - namespacedRef
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: when type is namespacedRef, namespacedRef must be set
                  rule: 'self.type == ''namespacedRef'' ? has(self.namespacedRef)
                    : true'
                - message: when type is namespacedRef, konnectID must not be set
                  rule: 'self.type == ''namespacedRef'' ? !has(self.konnectID) : true'
                - message: when type is konnectID, konnectID must be set
                  rule: 'self.type == ''konnectID'' ? has(self.konnectID) : true'
                - message: when type is konnectID, namespacedRef must not be set
                  rule: 'self.type == ''konnectID'' ? !has(self.namespacedRef) : true'
              apiSpec:
                properties:
                  apiKey:
                    properties:
                      secretRef:
                        properties:
                          key:
                            maxLength: 253
                            minLength: 1
                            type: string
                          name:
                            maxLength: 253
                            minLength: 1
                            type: string
                          namespace:
                            maxLength: 253
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      type:
                        default: inline
                        enum:
                        - inline
                        - secretRef
                        type: string
                      value:
                        maxLength: 4096
                        type: string
                    required:
                    - type
                    type: object
                    x-kubernetes-validations:
                    - message: value required when type=inline; secretRef required
                        when type=secretRef
                      rule: 'self.type == ''inline'' ? has(self.value) : has(self.secretRef)'
                  displayName:
                    maxLength: 253
                    minLength: 1
                    type: string
                  labels:
                    additionalProperties:
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                      type: string
                    type: object
                  managedBy:
                    additionalProperties:
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                      type: string
                    type: object
                  name:
                    maxLength: 256
                    minLength: 1
                    pattern: ^[A-Za-z0-9._:@-]{1,256}$
                    type: string
                  ttl:
                    type: integer
                  type:
                    enum:
                    - api-key
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - displayName
                - name
                - type
                type: object
            required:
            - aiGatewayConsumerRef
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consumerID:
                properties:
                  id:
                    maxLength: 256
                    type: string
                type: object
              gatewayID:
                properties:
                  id:
                    maxLength: 256
                    type: string
                type: object
              id:
                maxLength: 256
                type: string
              observedGeneration:
                format: int64
                type: integer
              organizationID:
                maxLength: 256
                type: string
              serverURL:
                maxLength: 512
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.aiGatewayConsumerRef is immutable when an entity is already
            Programmed
          rule: '!has(self.spec.aiGatewayConsumerRef) || !has(self.status.conditions)
            || !self.status.conditions.exists(c, c.type == ''Programmed'' && c.status
            == ''True'') || oldSelf.spec.aiGatewayConsumerRef == self.spec.aiGatewayConsumerRef'
    served: true
    storage: true
    subresources:
      status: {}

{{- end }}
