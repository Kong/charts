# This file is auto-generated by KO's hack/generators/conversion-webhook/main.go generator.
{{- if .Values.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
{{ if .Values.keep }}
    helm.sh/resource-policy: keep
{{ end }}
    kubernetes-configuration.konghq.com/channels: kong-operator
    kubernetes-configuration.konghq.com/version: v2.2.3
  name: portalcustomizations.konnect.konghq.com
spec:
  group: konnect.konghq.com
  names:
    categories:
    - konnect
    - kong
    kind: PortalCustomization
    listKind: PortalCustomizationList
    plural: portalcustomizations
    singular: portalcustomization
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The Resource is Programmed on Konnect
      jsonPath: .status.conditions[?(@.type=='Programmed')].status
      name: Programmed
      type: string
    - description: Konnect Organization ID this resource belongs to.
      jsonPath: .status.organizationID
      name: OrgID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PortalCustomization is the Schema for the portalcustomizations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PortalCustomizationSpec defines the desired state of PortalCustomization.
            properties:
              apiSpec:
                description: APISpec defines the desired state of the resource's API
                  spec fields.
                properties:
                  css:
                    maxLength: 253
                    type: string
                  layout:
                    maxLength: 253
                    type: string
                  menu:
                    description: Menu is a type alias.
                    properties:
                      footerBottom:
                        items:
                          description: PortalMenuItem is a type alias.
                          properties:
                            external:
                              description: When clicked, open the link in a new window
                              enum:
                              - Enabled
                              - Disabled
                              type: string
                            path:
                              description: The absolute path of a page in a portal
                                with a leading slash.
                              maxLength: 512
                              minLength: 1
                              type: string
                            title:
                              description: The link display text
                              maxLength: 253
                              minLength: 1
                              type: string
                            visibility:
                              description: |-
                                Whether a menu item is public or private.
                                Private menu items are only accessible to authenticated users.
                              enum:
                              - public
                              - private
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - external
                          - path
                          - title
                          - visibility
                          type: object
                        type: array
                      footerSections:
                        items:
                          description: PortalFooterMenuSection is a type alias.
                          properties:
                            items:
                              items:
                                description: PortalMenuItem is a type alias.
                                properties:
                                  external:
                                    description: When clicked, open the link in a
                                      new window
                                    enum:
                                    - Enabled
                                    - Disabled
                                    type: string
                                  path:
                                    description: The absolute path of a page in a
                                      portal with a leading slash.
                                    maxLength: 512
                                    minLength: 1
                                    type: string
                                  title:
                                    description: The link display text
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                  visibility:
                                    description: |-
                                      Whether a menu item is public or private.
                                      Private menu items are only accessible to authenticated users.
                                    enum:
                                    - public
                                    - private
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                required:
                                - external
                                - path
                                - title
                                - visibility
                                type: object
                              type: array
                            title:
                              description: The footer menu section title
                              maxLength: 512
                              minLength: 1
                              type: string
                          required:
                          - items
                          - title
                          type: object
                        type: array
                      main:
                        items:
                          description: PortalMenuItem is a type alias.
                          properties:
                            external:
                              description: When clicked, open the link in a new window
                              enum:
                              - Enabled
                              - Disabled
                              type: string
                            path:
                              description: The absolute path of a page in a portal
                                with a leading slash.
                              maxLength: 512
                              minLength: 1
                              type: string
                            title:
                              description: The link display text
                              maxLength: 253
                              minLength: 1
                              type: string
                            visibility:
                              description: |-
                                Whether a menu item is public or private.
                                Private menu items are only accessible to authenticated users.
                              enum:
                              - public
                              - private
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - external
                          - path
                          - title
                          - visibility
                          type: object
                        type: array
                    type: object
                  robots:
                    maxLength: 253
                    type: string
                  specRenderer:
                    description: SpecRenderer is a type alias.
                    properties:
                      allowCustomServerUrls:
                        description: |-
                          Let users define a custom server URL for endpoints.
                          This will be used to generate code snippets and to test the API.
                          The URL is client-side only and is not saved.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      hideDeprecated:
                        description: Manage visibility of deprecated endpoints and
                          models.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      hideInternal:
                        description: Manage visibility of internal endpoints and models.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      infiniteScroll:
                        description: |-
                          Display the full spec on a single, scrollable page.
                          If disabled, documentation, endpoints, and schemas appear on separate pages.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      showSchemas:
                        description: |-
                          Control whether schemas are visible in your API specs.
                          When enabled, schemas appear in the side navigation below the endpoints.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      tryItInsomnia:
                        description: |-
                          Enables users to open API specifications in Insomnia to explore and send
                          requests with the native client.
                          Only public API specifications are supported.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      tryItUi:
                        description: |-
                          Enable in-browser testing for your APIs.
                          All linked gateways must have the CORS plugin configured.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                    type: object
                  theme:
                    description: Theme is a type alias.
                    properties:
                      colors:
                        description: Colors is a type alias.
                        properties:
                          primary:
                            maxLength: 253
                            pattern: ^#([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$
                            type: string
                        type: object
                      mode:
                        enum:
                        - light
                        - dark
                        - system
                        maxLength: 253
                        type: string
                      name:
                        maxLength: 253
                        type: string
                    type: object
                type: object
              portalRef:
                description: PortalRef is the reference to the parent Portal object.
                properties:
                  konnectID:
                    description: |-
                      KonnectID is the schema for the KonnectID type.
                      This field is required when the Type is konnectID.
                    maxLength: 36
                    type: string
                  namespacedRef:
                    description: |-
                      NamespacedRef is a reference to a KeySet entity inside the cluster.
                      This field is required when the Type is namespacedRef.
                    properties:
                      name:
                        description: Name is the name of the referred resource.
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referred resource.

                          For namespace-scoped resources if no Namespace is provided then the
                          namespace of the parent object MUST be used.

                          This field MUST not be set when referring to cluster-scoped resources.
                        maxLength: 253
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    description: |-
                      Type defines type of the object which is referenced. It can be one of:

                      - konnectID
                      - namespacedRef
                    enum:
                    - konnectID
                    - namespacedRef
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: when type is namespacedRef, namespacedRef must be set
                  rule: 'self.type == ''namespacedRef'' ? has(self.namespacedRef)
                    : true'
                - message: when type is namespacedRef, konnectID must not be set
                  rule: 'self.type == ''namespacedRef'' ? !has(self.konnectID) : true'
                - message: when type is konnectID, konnectID must be set
                  rule: 'self.type == ''konnectID'' ? has(self.konnectID) : true'
                - message: when type is konnectID, namespacedRef must not be set
                  rule: 'self.type == ''konnectID'' ? !has(self.namespacedRef) : true'
            required:
            - portalRef
            type: object
          status:
            description: PortalCustomizationStatus defines the observed state of PortalCustomization.
            properties:
              conditions:
                description: Conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              id:
                description: |-
                  ID is the unique identifier of the Konnect entity as assigned by Konnect API.
                  If it's unset (empty string), it means the Konnect entity hasn't been created yet.
                maxLength: 256
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              organizationID:
                description: OrgID is ID of Konnect Org that this entity has been
                  created in.
                maxLength: 256
                type: string
              portalID:
                description: PortalID is the Konnect ID of the parent Portal.
                properties:
                  id:
                    description: ID is the unique identifier of the Konnect entity
                      as assigned by Konnect API.
                    maxLength: 256
                    type: string
                type: object
              serverURL:
                description: ServerURL is the URL of the Konnect server in which the
                  entity exists.
                maxLength: 512
                type: string
            type: object
        required:
        - metadata
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
