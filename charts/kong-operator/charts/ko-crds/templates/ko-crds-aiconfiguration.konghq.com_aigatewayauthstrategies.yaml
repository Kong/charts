# This file is auto-generated by KO's hack/generators/conversion-webhook/main.go generator.
{{- if .Values.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
{{ if .Values.keep }}
    helm.sh/resource-policy: keep
{{ end }}
    kubernetes-configuration.konghq.com/channels: kong-operator
    kubernetes-configuration.konghq.com/version: v2.3.0
  name: aigatewayauthstrategies.aiconfiguration.konghq.com
spec:
  group: aiconfiguration.konghq.com
  names:
    categories:
    - kong
    kind: AIGatewayAuthStrategy
    listKind: AIGatewayAuthStrategyList
    plural: aigatewayauthstrategies
    singular: aigatewayauthstrategy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Konnect ID
      jsonPath: .status.id
      name: ID
      type: string
    - description: The Resource is Programmed on Konnect
      jsonPath: .status.conditions[?(@.type=='Programmed')].status
      name: Programmed
      type: string
    - description: Konnect Organization ID this resource belongs to.
      jsonPath: .status.organizationID
      name: OrgID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIGatewayAuthStrategy is the Schema for the aigatewayauthstrategys
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIGatewayAuthStrategySpec defines the desired state of AIGatewayAuthStrategy.
            properties:
              aiGatewayRef:
                description: AIGatewayRef is the reference to the parent KonnectAIGateway
                  object.
                properties:
                  konnectID:
                    description: |-
                      KonnectID is the schema for the KonnectID type.
                      This field is required when the Type is konnectID.
                    maxLength: 36
                    type: string
                  namespacedRef:
                    description: |-
                      NamespacedRef is a reference to a KeySet entity inside the cluster.
                      This field is required when the Type is namespacedRef.
                    properties:
                      name:
                        description: Name is the name of the referred resource.
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referred resource.

                          For namespace-scoped resources if no Namespace is provided then the
                          namespace of the parent object MUST be used.

                          This field MUST not be set when referring to cluster-scoped resources.
                        maxLength: 253
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    description: |-
                      Type defines type of the object which is referenced. It can be one of:

                      - konnectID
                      - namespacedRef
                    enum:
                    - konnectID
                    - namespacedRef
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: when type is namespacedRef, namespacedRef must be set
                  rule: 'self.type == ''namespacedRef'' ? has(self.namespacedRef)
                    : true'
                - message: when type is namespacedRef, konnectID must not be set
                  rule: 'self.type == ''namespacedRef'' ? !has(self.konnectID) : true'
                - message: when type is konnectID, konnectID must be set
                  rule: 'self.type == ''konnectID'' ? has(self.konnectID) : true'
                - message: when type is konnectID, namespacedRef must not be set
                  rule: 'self.type == ''konnectID'' ? !has(self.namespacedRef) : true'
              apiSpec:
                description: APISpec defines the desired state of the resource's API
                  spec fields.
                properties:
                  key-auth:
                    description: KeyAuth configuration.
                    properties:
                      config:
                        description: |-
                          Configuration for the key-auth auth strategy.
                          For advanced use cases, additional config properties can be sent in the
                          request body.
                          See: https://developer.konghq.com/plugins/key-auth/reference/ for the list
                          of properties
                        properties:
                          hideCredentials:
                            description: |-
                              An optional boolean value telling the plugin to show or hide the credential
                              from the upstream service.
                              If true, the plugin strips the credential from the request.
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          keyInBody:
                            description: |-
                              If enabled, reads the request body.
                              Supported MIME types: application/www-form-urlencoded, application/json, and
                              multipart/form-data.
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          keyInHeader:
                            description: |-
                              If enabled (default), the plugin reads the request header and tries to find
                              the key in it.
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          keyInQuery:
                            description: |-
                              If enabled (default), the plugin reads the query parameter in the request
                              and tries to find the key in it.
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          keyNames:
                            description: |-
                              An array of strings containing the names of the keys to look for in the
                              request.
                            items:
                              type: string
                            type: array
                          principals:
                            description: |-
                              Authenticate against Kong Identity instead of local credentials.
                              Mutually exclusive with identity realms.
                            properties:
                              directory:
                                description: The Kong Identity directory instance
                                  to authenticate against.
                                maxLength: 253
                                pattern: ^[a-zA-Z0-9_-]+$
                                type: string
                              enabled:
                                description: When true, authenticate against Kong
                                  Identity instead of local credentials.
                                enum:
                                - Enabled
                                - Disabled
                                type: string
                              errorOnMiss:
                                description: |-
                                  When true (default), reject the request if no matching principal is found in
                                  Kong Identity.
                                  When false, allow the request to continue unauthenticated instead.
                                enum:
                                - Enabled
                                - Disabled
                                type: string
                            type: object
                        type: object
                      displayName:
                        description: The display name for this auth strategy instance.
                        maxLength: 256
                        minLength: 1
                        type: string
                      labels:
                        additionalProperties:
                          description: PublicLabelsValue is the value type for PublicLabels.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                          type: string
                        description: |-
                          Public labels store information about an entity that can be used for
                          filtering a list of objects.

                          Public labels are intended to store **PUBLIC** metadata.

                          Keys must be of length 1-63 characters, and cannot start with "kong",
                          "konnect", "mesh", "kic", or "_".
                        type: object
                      managedBy:
                        additionalProperties:
                          description: ManagedByValue is the value type for ManagedBy.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                          type: string
                        description: |-
                          Stores information about what manages this entity, such as the tool or
                          system responsible for its lifecycle (for example, `terraform`).

                          Keys must be 1–63 characters long and start with an alphanumeric
                          character.
                        type: object
                      name:
                        description: |-
                          **Pre-release Feature**
                          This feature is currently in beta and is subject to change.

                          A user-defined unique identifier for this auth strategy instance, used as a
                          stable human-readable reference.
                          This value is immutable after creation.
                        maxLength: 256
                        minLength: 1
                        pattern: ^[A-Za-z0-9._:-]{1,256}$
                        type: string
                    required:
                    - displayName
                    - name
                    type: object
                  openid-connect:
                    description: OpenIDConnect configuration.
                    properties:
                      config:
                        description: |-
                          Configuration for the OpenID Connect auth strategy.
                          For advanced use cases, additional config properties can be sent in the
                          request body.
                          See: https://developer.konghq.com/plugins/openid-connect/reference/ for the
                          list of properties
                        properties:
                          audienceRequired:
                            description: Audiences required in the access token or
                              introspection response.
                            items:
                              type: string
                            type: array
                          authMethods:
                            description: Types of credentials/grants to enable.
                            items:
                              type: string
                            type: array
                          cacheIntrospection:
                            description: Cache introspection endpoint requests.
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          cacheTokensSalt:
                            description: |-
                              Salt used for generating the cache key that is used for caching the token
                              endpoint requests.
                            maxLength: 253
                            minLength: 1
                            type: string
                          clientAlg:
                            description: |-
                              Algorithm to use for `client_secret_jwt` or `private_key_jwt`
                              authentication.
                            items:
                              type: string
                            type: array
                          clientAuth:
                            description: Client authentication methods used with the
                              identity provider.
                            items:
                              type: string
                            type: array
                          clientID:
                            description: |-
                              An array of strings representing the client id for the OpenID Connect
                              provider.
                              When multiple values are provided, the client ID and secrets pairs
                              correspond based on their locations in the array.
                            items:
                              type: string
                            type: array
                          clientSecret:
                            description: |-
                              An array of strings representing the client secret for the OpenID Connect
                              provider.
                              When multiple values are provided, the client ID and secrets pairs
                              correspond based on their locations in the array.
                            items:
                              description: |-
                                SensitiveDataSource holds a sensitive string value that can be provided
                                either inline or sourced from a Kubernetes Secret.
                              properties:
                                secretRef:
                                  description: |-
                                    SecretRef is a reference to a Kubernetes Secret containing the sensitive data.
                                    Required when type is 'secretRef'.
                                  properties:
                                    key:
                                      description: Key is the data key within the
                                        Secret.
                                      maxLength: 253
                                      minLength: 1
                                      type: string
                                    name:
                                      description: Name is the name of the referred
                                        resource.
                                      maxLength: 253
                                      minLength: 1
                                      type: string
                                    namespace:
                                      description: |-
                                        Namespace is the namespace of the referred resource.

                                        For namespace-scoped resources if no Namespace is provided then the
                                        namespace of the parent object MUST be used.

                                        This field MUST not be set when referring to cluster-scoped resources.
                                      maxLength: 253
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                type:
                                  default: inline
                                  description: 'Type indicates the source of the sensitive
                                    data: ''inline'' or ''secretRef''.'
                                  enum:
                                  - inline
                                  - secretRef
                                  type: string
                                value:
                                  description: |-
                                    Value contains the sensitive data provided inline.
                                    Required when type is 'inline'.
                                  maxLength: 4096
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: value required when type=inline; secretRef
                                  required when type=secretRef
                                rule: 'self.type == ''inline'' ? has(self.value) :
                                  has(self.secretRef)'
                            type: array
                          consumerBy:
                            description: Consumer fields used when mapping a token
                              claim to a Kong consumer.
                            items:
                              type: string
                            type: array
                          consumerClaims:
                            description: |-
                              An array containing an array of string paths representing the location of
                              the claim in a nested object.
                              For example, to map to user.info.id, set [ "user", "info", "id" ].
                            items:
                              items:
                                type: string
                              type: array
                            type: array
                          consumerGroupsClaim:
                            description: |-
                              The claim used for consumer groups mapping.
                              If multiple values are set, it means the claim is inside a nested object of
                              the token payload.
                            items:
                              type: string
                            type: array
                          consumerGroupsOptional:
                            description: Do not terminate the request if consumer
                              groups mapping fails.
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          consumerOptional:
                            description: Do not terminate the request if consumer
                              mapping fails.
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          credentialClaim:
                            description: |-
                              Claim path used to derive virtual credentials when consumer mapping is not
                              used.
                            items:
                              type: string
                            type: array
                          hideCredentials:
                            description: |-
                              Remove credentials used for authentication before proxying the request
                              upstream.
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          httpProxy:
                            description: HTTP proxy used for identity provider requests.
                            maxLength: 253
                            type: string
                          httpProxyAuthorization:
                            description: Authorization header value sent to the HTTP
                              proxy.
                            maxLength: 253
                            type: string
                          httpVersion:
                            description: HTTP version used for identity provider requests.
                            type: number
                          httpsProxy:
                            description: HTTPS proxy used for identity provider requests.
                            maxLength: 253
                            type: string
                          httpsProxyAuthorization:
                            description: Authorization header value sent to the HTTPS
                              proxy.
                            maxLength: 253
                            type: string
                          introspectionEndpoint:
                            description: Overrides the introspection endpoint returned
                              by discovery.
                            maxLength: 253
                            type: string
                          issuer:
                            description: URL that identifies the OpenID Provider
                            maxLength: 253
                            type: string
                          jwksEndpoint:
                            description: Overrides the JWKS endpoint returned by discovery.
                            maxLength: 253
                            type: string
                          keepalive:
                            description: Reuse HTTP client connections for identity
                              provider requests.
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          leeway:
                            description: Leeway, in seconds, for validating token
                              time claims.
                            minimum: 0
                            type: integer
                          mtlsIntrospectionEndpoint:
                            description: mTLS alias for the introspection endpoint.
                            maxLength: 253
                            type: string
                          noProxy:
                            description: Comma-separated hosts that bypass the configured
                              proxies.
                            maxLength: 253
                            type: string
                          principals:
                            description: Map a request to a Kong Identity principal
                              after token verification.
                            properties:
                              directory:
                                description: The Kong Identity directory instance
                                  to look up against.
                                maxLength: 253
                                pattern: ^[a-zA-Z0-9_-]+$
                                type: string
                              enabled:
                                description: When true, look up a Kong Identity principal
                                  after token verification.
                                enum:
                                - Enabled
                                - Disabled
                                type: string
                              errorOnMiss:
                                description: |-
                                  When true (default), reject the request if no principal is matched in Kong
                                  Identity after token
                                  verification.
                                  When false, the request continues without an authenticated principal set.
                                enum:
                                - Enabled
                                - Disabled
                                type: string
                              matchConsumer:
                                description: |-
                                  If a consumer is attached to the matched principal, load it and set it in
                                  the request context,
                                  overriding consumer_by.
                                enum:
                                - Enabled
                                - Disabled
                                type: string
                              matchConsumerGroups:
                                description: |-
                                  If consumer groups are attached to the matched principal, load them,
                                  overriding consumer_groups_claim.
                                enum:
                                - Enabled
                                - Disabled
                                type: string
                              principalBy:
                                description: |-
                                  Custom identity name for a custom Kong Identity lookup.
                                  When absent and principal_claim is set,
                                  a lookup is performed using principal_claim as the claim name instead of the
                                  default sub claim.
                                maxLength: 253
                                minLength: 1
                                type: string
                              principalClaim:
                                description: |-
                                  Token claim used for the Kong Identity lookup.
                                  If multiple values are set, the claim is inside a
                                  nested object of the token payload.
                                  Used together with, or instead of, principal_by.
                                items:
                                  type: string
                                type: array
                            type: object
                          scopes:
                            description: This field is referenceable.
                            items:
                              type: string
                            type: array
                          sslVerify:
                            enum:
                            - Enabled
                            - Disabled
                            type: string
                          timeout:
                            description: Network I/O timeout, in milliseconds, for
                              identity provider requests.
                            minimum: 0
                            type: integer
                          upstreamHeaders:
                            description: Map token claims to upstream headers using
                              path-based access.
                            items:
                              description: |-
                                AIGatewayAuthStrategyOpenIDConnectConfigUpstreamHeaders Map token claims to
                                upstream headers using path-based access.
                              properties:
                                header:
                                  description: The name of the header.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                path:
                                  description: The path of the header value.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - header
                              - path
                              type: object
                            type: array
                        required:
                        - cacheTokensSalt
                        type: object
                      displayName:
                        description: The display name for this auth strategy instance.
                        maxLength: 256
                        minLength: 1
                        type: string
                      labels:
                        additionalProperties:
                          description: PublicLabelsValue is the value type for PublicLabels.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                          type: string
                        description: |-
                          Public labels store information about an entity that can be used for
                          filtering a list of objects.

                          Public labels are intended to store **PUBLIC** metadata.

                          Keys must be of length 1-63 characters, and cannot start with "kong",
                          "konnect", "mesh", "kic", or "_".
                        type: object
                      managedBy:
                        additionalProperties:
                          description: ManagedByValue is the value type for ManagedBy.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                          type: string
                        description: |-
                          Stores information about what manages this entity, such as the tool or
                          system responsible for its lifecycle (for example, `terraform`).

                          Keys must be 1–63 characters long and start with an alphanumeric
                          character.
                        type: object
                      name:
                        description: |-
                          **Pre-release Feature**
                          This feature is currently in beta and is subject to change.

                          A user-defined unique identifier for this auth strategy instance, used as a
                          stable human-readable reference.
                          This value is immutable after creation.
                        maxLength: 256
                        minLength: 1
                        pattern: ^[A-Za-z0-9._:-]{1,256}$
                        type: string
                    required:
                    - displayName
                    - name
                    type: object
                  type:
                    description: Type designates the type of configuration.
                    enum:
                    - key-auth
                    - openid-connect
                    minLength: 1
                    type: string
                required:
                - type
                type: object
            required:
            - aiGatewayRef
            type: object
          status:
            description: AIGatewayAuthStrategyStatus defines the observed state of
              AIGatewayAuthStrategy.
            properties:
              conditions:
                description: Conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              gatewayID:
                description: GatewayID is the Konnect ID of the parent Gateway.
                properties:
                  id:
                    description: ID is the unique identifier of the Konnect entity
                      as assigned by Konnect API.
                    maxLength: 256
                    type: string
                type: object
              id:
                description: |-
                  ID is the unique identifier of the Konnect entity as assigned by Konnect API.
                  If it's unset (empty string), it means the Konnect entity hasn't been created yet.
                maxLength: 256
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              organizationID:
                description: OrgID is ID of Konnect Org that this entity has been
                  created in.
                maxLength: 256
                type: string
              serverURL:
                description: ServerURL is the URL of the Konnect server in which the
                  entity exists.
                maxLength: 512
                type: string
            type: object
        required:
        - metadata
        type: object
        x-kubernetes-validations:
        - message: spec.aiGatewayRef is immutable when an entity is already Programmed
          rule: '!has(self.spec.aiGatewayRef) || !has(self.status.conditions) || !self.status.conditions.exists(c,
            c.type == ''Programmed'' && c.status == ''True'') || oldSelf.spec.aiGatewayRef
            == self.spec.aiGatewayRef'
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
