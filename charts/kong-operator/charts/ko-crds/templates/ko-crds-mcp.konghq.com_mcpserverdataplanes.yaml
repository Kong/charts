# This file is auto-generated by KO's hack/generators/conversion-webhook/main.go generator.
{{- if .Values.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
{{ if .Values.keep }}
    helm.sh/resource-policy: keep
{{ end }}
    kubernetes-configuration.konghq.com/channels: kong-operator
    kubernetes-configuration.konghq.com/version: v2.3.0
  name: mcpserverdataplanes.mcp.konghq.com
spec:
  group: mcp.konghq.com
  names:
    categories:
    - kong
    kind: MCPServerDataPlane
    listKind: MCPServerDataPlaneList
    plural: mcpserverdataplanes
    shortNames:
    - mcpdp
    singular: mcpserverdataplane
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The Resource is ready
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          MCPServerDataPlane is the Schema for the MCP Server data planes API.
          It manages a MCP Server Deployment that uses Konnect control plane configuration
          via a referenced MCPServer resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of MCPServerDataPlane.
            properties:
              deployment:
                description: |-
                  Deployment configures the Deployment: image, replicas, resources,
                  extra env vars, volume mounts, etc.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations are custom annotations that are propagated to the MCP server
                      Deployment metadata by the operator.
                    maxProperties: 64
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels are custom labels that are propagated to the MCP server Deployment
                      metadata by the operator.
                    maxProperties: 64
                    type: object
                  podTemplateSpec:
                    description: PodTemplateSpec defines PodTemplateSpec for managed
                      Deployment's Pods.
                    properties:
                      metadata:
                        description: Metadata is the metadata of the pod template
                          spec.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: |-
                              Annotations is an unstructured key value map stored with a resource that may be
                              set by external tools to store and retrieve arbitrary metadata. They are not
                              queryable and should be preserved when modifying objects.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations
                            maxProperties: 64
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            description: |-
                              Map of string keys and values that can be used to organize and categorize
                              (scope and select) objects. May match selectors of replication controllers
                              and services.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels
                            maxProperties: 64
                            type: object
                        type: object
                      spec:
                        description: Specification of the desired behavior of the
                          pod.
                        properties:
                          containers:
                            description: |-
                              List of containers belonging to the pod to patch.
                              A container is patched by name.
                            items:
                              description: |-
                                MCPServerDataPlaneContainer describes a single container that
                                you want to run within a pod, exposes fields supported by the
                                MCPServerDataPlane.
                              properties:
                                name:
                                  description: |-
                                    Name of the container. Must match the name of a container in the Pod
                                    managed by the MCPServerDataPlane controller (e.g. "mcp-server").
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                resources:
                                  description: Resources specifies the compute resource
                                    requirements for the container.
                                  properties:
                                    claims:
                                      description: |-
                                        Claims lists the names of resources, defined in spec.resourceClaims,
                                        that are used by this container.

                                        This field depends on the
                                        DynamicResourceAllocation feature gate.

                                        This field is immutable. It can only be set for containers.
                                      items:
                                        description: ResourceClaim references one
                                          entry in PodSpec.ResourceClaims.
                                        properties:
                                          name:
                                            description: |-
                                              Name must match the name of one entry in pod.spec.resourceClaims of
                                              the Pod where this field is used. It makes that resource available
                                              inside a container.
                                            type: string
                                          request:
                                            description: |-
                                              Request is the name chosen for a request in the referenced claim.
                                              If empty, everything from the claim is made available, otherwise
                                              only the result of this request.
                                            type: string
                                        required:
                                        - name
                                        type: object
                                      type: array
                                      x-kubernetes-list-map-keys:
                                      - name
                                      x-kubernetes-list-type: map
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: |-
                                        Limits describes the maximum amount of compute resources allowed.
                                        More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: |-
                                        Requests describes the minimum amount of compute resources required.
                                        If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                        otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                        More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                      type: object
                                  type: object
                              required:
                              - name
                              type: object
                            maxItems: 16
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                        type: object
                    type: object
                  replicas:
                    default: 1
                    description: Replicas describes the number of desired pods.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              mcpServerRef:
                description: |-
                  MCPServerRef references the control plane this MCPServerDataPlane connects to.
                  The type field identifies which kind of MCPServer is being referenced.
                  Currently only konnectNamespacedRef is supported, which references a
                  MCPServer resource in the same namespace.
                properties:
                  konnectNamespacedRef:
                    description: |-
                      KonnectNamespacedRef references a MCPServer resource in the same namespace.
                      Must be set when type is konnectNamespacedRef; validated by CEL rules on this struct.
                    properties:
                      name:
                        description: Name is the name of the MCPServer resource.
                        maxLength: 253
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    description: |-
                      Type indicates the type of the control plane being referenced.
                      Currently only konnectNamespacedRef is supported.
                    enum:
                    - konnectNamespacedRef
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: konnectNamespacedRef must be set when type is konnectNamespacedRef
                  rule: 'self.type == ''konnectNamespacedRef'' ? has(self.konnectNamespacedRef)
                    : true'
            required:
            - mcpServerRef
            type: object
          status:
            description: Status defines the observed state of MCPServerDataPlane.
            properties:
              conditions:
                default:
                - lastTransitionTime: "1970-01-01T00:00:00Z"
                  message: Waiting for controller
                  reason: Pending
                  status: Unknown
                  type: Ready
                description: Conditions describe the status of the MCPServerDataPlane.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              readyReplicas:
                default: 0
                description: ReadyReplicas indicates how many replicas have reported
                  to be ready.
                format: int32
                type: integer
              replicas:
                default: 0
                description: Replicas indicates how many replicas have been set for
                  the MCPServerDataPlane.
                format: int32
                type: integer
              selector:
                description: |-
                  Selector is the label selector used by the scale subresource to match
                  the Pods managed by the owned Deployment.
                maxLength: 256
                type: string
              version:
                description: Version indicates the version of the referenced, remote
                  MCPServer.
                maxLength: 256
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.deployment.replicas
        statusReplicasPath: .status.replicas
      status: {}
{{- end }}
