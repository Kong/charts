# This file is auto-generated by KO's hack/generators/conversion-webhook/main.go generator.
{{- if .Values.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
{{ if .Values.keep }}
    helm.sh/resource-policy: keep
{{ end }}
    kubernetes-configuration.konghq.com/channels: kong-operator
    kubernetes-configuration.konghq.com/version: v2.2.3
  name: eventgatewayvirtualclusters.configuration.konghq.com
spec:
  group: configuration.konghq.com
  names:
    categories:
    - kong
    kind: EventGatewayVirtualCluster
    listKind: EventGatewayVirtualClusterList
    plural: eventgatewayvirtualclusters
    singular: eventgatewayvirtualcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Konnect ID
      jsonPath: .status.id
      name: ID
      type: string
    - description: The Resource is Programmed on Konnect
      jsonPath: .status.conditions[?(@.type=='Programmed')].status
      name: Programmed
      type: string
    - description: Konnect Organization ID this resource belongs to.
      jsonPath: .status.organizationID
      name: OrgID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EventGatewayVirtualCluster is the Schema for the eventgatewayvirtualclusters
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventGatewayVirtualClusterSpec defines the desired state
              of EventGatewayVirtualCluster.
            properties:
              apiSpec:
                description: APISpec defines the desired state of the resource's API
                  spec fields.
                properties:
                  aclMode:
                    description: |-
                      Configures whether or not ACL policies are enforced on the gateway.
                      - `enforce_on_gateway` means the gateway enforces its own ACL policies for
                      this virtual cluster

                      and does not forward ACL-related commands to the backend cluster.
                      Note that if there are no ACL policies configured, all access is denied.
                      - `passthrough` tells the gateway to forward all ACL-related commands.
                    enum:
                    - enforce_on_gateway
                    - passthrough
                    maxLength: 253
                    minLength: 1
                    type: string
                  authentication:
                    description: |-
                      How to handle authentication from clients.

                      It tries to authenticate with every rule sequentially one by one.
                      It succeeds on the first match, and fails if no rule matches.
                    items:
                      description: |-
                        VirtualClusterAuthenticationScheme represents a union type for VirtualClusterAuthenticationScheme.
                        Only one of the fields should be set based on the Type.
                      properties:
                        anonymous:
                          description: Anonymous configuration.
                          type: object
                        clientCertificate:
                          description: ClientCertificate configuration.
                          type: object
                        oauthBearer:
                          description: OauthBearer configuration.
                          properties:
                            claimsMapping:
                              description: |-
                                Maps JWT claims in the case when sub and scope are presented as different
                                claims in your JWT token.
                              properties:
                                scope:
                                  description: Maps the scope claim.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                sub:
                                  description: Maps the subject claim.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                              type: object
                            jwks:
                              description: JSON Web Key Set configuration for verifying
                                token signatures.
                              properties:
                                cacheExpiration:
                                  description: Duration after which the gateway will
                                    fetch and cache JWKS.
                                  maxLength: 253
                                  type: string
                                endpoint:
                                  description: URL for JWKS endpoint.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                timeout:
                                  description: |-
                                    Total time from establishing connection to receive a response from JWKS
                                    endpoint.
                                  maxLength: 253
                                  type: string
                              required:
                              - endpoint
                              type: object
                            mediation:
                              description: |-
                                Methods to mediate authentication:
                                * passthrough - pass authentication from the client through proxy to the
                                backend cluster without any kind of

                                validation
                                * validate_forward - pass authentication from the client through proxy to
                                the backend cluster.

                                Proxy does the validation before forwarding it to the client.
                                * terminate - terminate authentication at the proxy level and originate
                                authentication to the backend cluster

                                using the configuration defined at BackendCluster's authentication.
                                SASL auth is not originated if authentication on the backend_cluster is not
                                configured.
                              enum:
                              - passthrough
                              - validate_forward
                              - terminate
                              maxLength: 253
                              minLength: 1
                              type: string
                            validate:
                              description: Validation rules.
                              properties:
                                audiences:
                                  description: |-
                                    List of expected audience values.
                                    One of them has to match the audience claim in the token.
                                  items:
                                    description: VirtualClusterAuthenticationAudience
                                      is a type alias.
                                    properties:
                                      name:
                                        maxLength: 253
                                        minLength: 1
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  type: array
                                issuer:
                                  description: Expected token issuer in the token.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                              type: object
                          required:
                          - mediation
                          type: object
                        saslPlain:
                          description: SaslPlain configuration.
                          properties:
                            mediation:
                              description: The mediation type for SASL/PLAIN authentication.
                              enum:
                              - passthrough
                              - terminate
                              maxLength: 253
                              minLength: 1
                              type: string
                            principals:
                              description: |-
                                List of principals to be able to authenticate with, used with `terminate`
                                mediation.
                              items:
                                description: |-
                                  VirtualClusterAuthenticationPrincipal A principal for authentication
                                  containing username and password.
                                properties:
                                  password:
                                    description: |-
                                      A sensitive value containing the secret or a reference to a secret as a
                                      template string expression.
                                      If the value is provided as plain text, it is encrypted at rest and omitted
                                      from API responses.
                                      If provided as an expression, the expression itself is stored and returned
                                      by the API.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                  username:
                                    description: |-
                                      A literal value or a reference to an existing secret as a template string
                                      expression.
                                      The value is stored and returned by the API as-is, not treated as sensitive
                                      information.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                required:
                                - password
                                - username
                                type: object
                              type: array
                          required:
                          - mediation
                          type: object
                        saslScram:
                          description: SaslScram configuration.
                          properties:
                            algorithm:
                              description: The algorithm used for SASL/SCRAM authentication.
                              enum:
                              - sha256
                              - sha512
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - algorithm
                          type: object
                        type:
                          description: Type designates the type of configuration.
                          enum:
                          - anonymous
                          - clientCertificate
                          - oauthBearer
                          - saslPlain
                          - saslScram
                          minLength: 1
                          type: string
                      required:
                      - type
                      type: object
                    type: array
                  description:
                    description: A human-readable description of the virtual cluster.
                    maxLength: 512
                    type: string
                  dnsLabel:
                    description: |-
                      The DNS label used in the bootstrap server URL to identify the virtual
                      cluster when using SNI routing.
                      The format follows the RFC1035: 1-63 chars, lowercase alphanumeric or '-',
                      must start and end with an alphanumeric character.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  labels:
                    additionalProperties:
                      description: LabelsValue is the value type for Labels.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                      type: string
                    description: |-
                      Labels store metadata of an entity that can be used for filtering an entity
                      list or for searching across entity types.

                      Keys must be of length 1-63 characters, and cannot start with "kong",
                      "konnect", "mesh", "kic", or "_".
                    type: object
                  name:
                    description: The name of the virtual cluster.
                    maxLength: 255
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace allows to implement multitenancy using a single backend cluster.
                      It allows to either hide or enforce a static prefix on resources (topics,
                      consumer group IDs, transaction IDs).
                    properties:
                      additional:
                        description: VirtualClusterNamespaceAdditionalProperties is
                          a type alias.
                        properties:
                          consumerGroups:
                            description: |-
                              Consumer group IDs to expose even if they don't start with the namespace
                              prefix.
                            items:
                              description: |-
                                VirtualClusterNamespaceIDSelector represents a union type for VirtualClusterNamespaceIDSelector.
                                Only one of the fields should be set based on the Type.
                              properties:
                                exactList:
                                  description: ExactList configuration.
                                  properties:
                                    exactList:
                                      items:
                                        description: VirtualClusterNamespaceIDSelectorExactListExactList
                                          is a type alias.
                                        properties:
                                          value:
                                            maxLength: 253
                                            minLength: 1
                                            type: string
                                        required:
                                        - value
                                        type: object
                                      type: array
                                  type: object
                                glob:
                                  description: Glob configuration.
                                  properties:
                                    glob:
                                      description: Expose any id that matches this
                                        glob pattern (e.g., `my_id_*`).
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[A-Za-z0-9._?*-]+$
                                      type: string
                                  required:
                                  - glob
                                  type: object
                                type:
                                  description: Type designates the type of configuration.
                                  enum:
                                  - exactList
                                  - glob
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                            type: array
                          topics:
                            description: |-
                              Additional backend topics to expose even if they don't match the namespace
                              prefix.
                              The topics are not affected by the hide/enforce prefix mode.
                              If the client tries to create a topic that matches this list, the request is
                              rejected.
                            items:
                              description: |-
                                VirtualClusterNamespaceTopicSelector represents a union type for VirtualClusterNamespaceTopicSelector.
                                Only one of the fields should be set based on the Type.
                              properties:
                                exactList:
                                  description: ExactList configuration.
                                  properties:
                                    conflict:
                                      description: |-
                                        How to inform the user about conflicts where multiple backend topics would
                                        map to the same virtual topic name.
                                        * warn - log in the Event Gateway logs.
                                        Additionally, it sets knep_namespace_topic_conflict to 1.
                                        * ignore - do not do anything.
                                        It does not cause knep_namespace_topic_conflict metric to be set to 1.
                                      enum:
                                      - warn
                                      - ignore
                                      maxLength: 253
                                      type: string
                                    exactList:
                                      description: Explicit allow-list of backend
                                        topic names.
                                      items:
                                        description: NamespaceExactAllowListItem is
                                          a type alias.
                                        properties:
                                          backend:
                                            maxLength: 253
                                            minLength: 1
                                            type: string
                                        required:
                                        - backend
                                        type: object
                                      type: array
                                  type: object
                                glob:
                                  description: Glob configuration.
                                  properties:
                                    conflict:
                                      description: |-
                                        How to inform the user about conflicts where multiple backend topics would
                                        map to the same virtual topic name.
                                        * warn - log in the Event Gateway logs.
                                        Additionally, it sets knep_namespace_topic_conflict to 1.
                                        * ignore - do not do anything.
                                        It does not cause knep_namespace_topic_conflict metric to be set to 1.
                                      enum:
                                      - warn
                                      - ignore
                                      maxLength: 253
                                      type: string
                                    glob:
                                      description: |-
                                        Expose any backend topic that matches this glob pattern (e.g.,
                                        `operations_data_*`).
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[A-Za-z0-9._?*-]+$
                                      type: string
                                  required:
                                  - glob
                                  type: object
                                type:
                                  description: Type designates the type of configuration.
                                  enum:
                                  - exactList
                                  - glob
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                            type: array
                        type: object
                      mode:
                        description: |-
                          * hide_prefix - the configured prefix is hidden from clients for topics and
                          IDs when reading.

                          Created resources are written with the prefix on the backend cluster.
                          * enforce_prefix - the configured prefix remains visible to clients.

                          Created resources must include the prefix or the request will fail.
                        enum:
                        - hide_prefix
                        - enforce_prefix
                        maxLength: 253
                        minLength: 1
                        type: string
                      prefix:
                        description: |-
                          The namespace is differentiated by this chosen prefix.
                          For example, if the prefix is set to "analytics_" the topic named
                          "analytics_user_clicks" is available to the clients
                          of the virtual cluster.
                          Topics without the prefix will be ignored unless added via
                          `additional.topics`.
                        maxLength: 253
                        minLength: 1
                        type: string
                    required:
                    - mode
                    - prefix
                    type: object
                  topicAliases:
                    description: |-
                      **Pre-release Feature**
                      This feature is currently in beta and is subject to change.

                      Topic aliases allow exposing backend topics under additional names.
                      An alias creates a new entry point to the same physical data.
                      The alias `topic` field references namespace-visible names (if namespace is
                      configured).
                      Aliases are independent of namespace and can be used without it.

                      **Requires a minimum runtime version of `1.2`**.
                    items:
                      description: |-
                        VirtualClusterTopicAlias **Pre-release Feature**
                        This feature is currently in beta and is subject to change.

                        A topic alias maps an alias name to a namespace-visible topic name.
                        Clients can produce to, consume from, and discover the topic under the alias
                        name.
                        The original topic name remains accessible.

                        **Requires a minimum runtime version of `1.2`**.
                      properties:
                        alias:
                          description: The client-visible topic name.
                          maxLength: 253
                          minLength: 1
                          type: string
                        conflict:
                          description: |-
                            How to handle conflicts where an alias shadows a physical topic.
                            * warn - activate the alias but log a warning and set the conflict metric to
                            1.
                            * ignore - activate the alias silently.
                          enum:
                          - warn
                          - ignore
                          maxLength: 253
                          type: string
                        match:
                          description: |-
                            CEL expression evaluated against the connection's auth context.
                            If omitted or empty, the alias is active for all connections.
                          maxLength: 253
                          type: string
                        topic:
                          description: The namespace-visible topic name this alias
                            resolves to.
                          maxLength: 253
                          minLength: 1
                          type: string
                      required:
                      - alias
                      - topic
                      type: object
                    type: array
                required:
                - aclMode
                - authentication
                - dnsLabel
                - name
                type: object
              eventGatewayBackendClusterRef:
                description: EventGatewayBackendClusterRef is the reference to the
                  parent EventGatewayBackendCluster object.
                properties:
                  konnectID:
                    description: |-
                      KonnectID is the schema for the KonnectID type.
                      This field is required when the Type is konnectID.
                    maxLength: 36
                    type: string
                  namespacedRef:
                    description: |-
                      NamespacedRef is a reference to a KeySet entity inside the cluster.
                      This field is required when the Type is namespacedRef.
                    properties:
                      name:
                        description: Name is the name of the referred resource.
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referred resource.

                          For namespace-scoped resources if no Namespace is provided then the
                          namespace of the parent object MUST be used.

                          This field MUST not be set when referring to cluster-scoped resources.
                        maxLength: 253
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    description: |-
                      Type defines type of the object which is referenced. It can be one of:

                      - konnectID
                      - namespacedRef
                    enum:
                    - konnectID
                    - namespacedRef
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: when type is namespacedRef, namespacedRef must be set
                  rule: 'self.type == ''namespacedRef'' ? has(self.namespacedRef)
                    : true'
                - message: when type is namespacedRef, konnectID must not be set
                  rule: 'self.type == ''namespacedRef'' ? !has(self.konnectID) : true'
                - message: when type is konnectID, konnectID must be set
                  rule: 'self.type == ''konnectID'' ? has(self.konnectID) : true'
                - message: when type is konnectID, namespacedRef must not be set
                  rule: 'self.type == ''konnectID'' ? !has(self.namespacedRef) : true'
            required:
            - eventGatewayBackendClusterRef
            type: object
          status:
            description: EventGatewayVirtualClusterStatus defines the observed state
              of EventGatewayVirtualCluster.
            properties:
              conditions:
                description: Conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              eventGatewayBackendCluster:
                description: EventGatewayBackendCluster is the Konnect entity reference
                  for the parent EventGatewayBackendCluster.
                properties:
                  id:
                    description: ID is the unique identifier of the Konnect entity
                      as assigned by Konnect API.
                    maxLength: 256
                    type: string
                type: object
              gatewayID:
                description: GatewayID is the Konnect ID of the parent Gateway.
                properties:
                  id:
                    description: ID is the unique identifier of the Konnect entity
                      as assigned by Konnect API.
                    maxLength: 256
                    type: string
                type: object
              id:
                description: |-
                  ID is the unique identifier of the Konnect entity as assigned by Konnect API.
                  If it's unset (empty string), it means the Konnect entity hasn't been created yet.
                maxLength: 256
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              organizationID:
                description: OrgID is ID of Konnect Org that this entity has been
                  created in.
                maxLength: 256
                type: string
              serverURL:
                description: ServerURL is the URL of the Konnect server in which the
                  entity exists.
                maxLength: 512
                type: string
            type: object
        required:
        - metadata
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
