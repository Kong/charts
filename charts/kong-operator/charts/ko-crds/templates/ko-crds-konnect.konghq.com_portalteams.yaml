# This file is auto-generated by KO's hack/generators/conversion-webhook/main.go generator.
{{- if .Values.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
{{ if .Values.keep }}
    helm.sh/resource-policy: keep
{{ end }}
    kubernetes-configuration.konghq.com/channels: kong-operator
    kubernetes-configuration.konghq.com/version: v2.4.0-rapid.1
  name: portalteams.konnect.konghq.com
spec:
  group: konnect.konghq.com
  names:
    categories:
    - konnect
    - kong
    kind: PortalTeam
    listKind: PortalTeamList
    plural: portalteams
    singular: portalteam
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Konnect ID
      jsonPath: .status.id
      name: ID
      type: string
    - description: The Resource is Programmed on Konnect
      jsonPath: .status.conditions[?(@.type=='Programmed')].status
      name: Programmed
      type: string
    - description: Konnect Organization ID this resource belongs to.
      jsonPath: .status.organizationID
      name: OrgID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              apiSpec:
                properties:
                  canOwnApplications:
                    enum:
                    - Enabled
                    - Disabled
                    type: string
                  description:
                    maxLength: 250
                    minLength: 1
                    type: string
                  konnectManaged:
                    enum:
                    - Enabled
                    - Disabled
                    type: string
                  name:
                    maxLength: 250
                    minLength: 1
                    pattern: ^[\w \W]+$
                    type: string
                required:
                - name
                type: object
              portalRef:
                properties:
                  konnectID:
                    maxLength: 36
                    type: string
                  namespacedRef:
                    properties:
                      name:
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        maxLength: 253
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    enum:
                    - konnectID
                    - namespacedRef
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: when type is namespacedRef, namespacedRef must be set
                  rule: 'self.type == ''namespacedRef'' ? has(self.namespacedRef)
                    : true'
                - message: when type is namespacedRef, konnectID must not be set
                  rule: 'self.type == ''namespacedRef'' ? !has(self.konnectID) : true'
                - message: when type is konnectID, konnectID must be set
                  rule: 'self.type == ''konnectID'' ? has(self.konnectID) : true'
                - message: when type is konnectID, namespacedRef must not be set
                  rule: 'self.type == ''konnectID'' ? !has(self.namespacedRef) : true'
            required:
            - portalRef
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              id:
                maxLength: 256
                type: string
              observedGeneration:
                format: int64
                type: integer
              organizationID:
                maxLength: 256
                type: string
              portalID:
                properties:
                  id:
                    maxLength: 256
                    type: string
                type: object
              serverURL:
                maxLength: 512
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.portalRef is immutable when an entity is already Programmed
          rule: '!has(self.spec.portalRef) || !has(self.status.conditions) || !self.status.conditions.exists(c,
            c.type == ''Programmed'' && c.status == ''True'') || oldSelf.spec.portalRef
            == self.spec.portalRef'
    served: true
    storage: true
    subresources:
      status: {}

{{- end }}
