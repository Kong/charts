# This file is auto-generated by KO's hack/generators/conversion-webhook/main.go generator.
{{- if .Values.enabled }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
{{ if .Values.keep }}
    helm.sh/resource-policy: keep
{{ end }}
    kubernetes-configuration.konghq.com/channels: kong-operator
    kubernetes-configuration.konghq.com/version: v2.2.1
  name: eventgatewayvirtualclusterproducepolicies.configuration.konghq.com
spec:
  group: configuration.konghq.com
  names:
    categories:
    - kong
    kind: EventGatewayVirtualClusterProducePolicy
    listKind: EventGatewayVirtualClusterProducePolicyList
    plural: eventgatewayvirtualclusterproducepolicies
    singular: eventgatewayvirtualclusterproducepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Konnect ID
      jsonPath: .status.id
      name: ID
      type: string
    - description: The Resource is Programmed on Konnect
      jsonPath: .status.conditions[?(@.type=='Programmed')].status
      name: Programmed
      type: string
    - description: Konnect Organization ID this resource belongs to.
      jsonPath: .status.organizationID
      name: OrgID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EventGatewayVirtualClusterProducePolicy is the Schema for the
          eventgatewayvirtualclusterproducepolicys API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventGatewayVirtualClusterProducePolicySpec defines the desired
              state of EventGatewayVirtualClusterProducePolicy.
            properties:
              apiSpec:
                description: APISpec defines the desired state of the resource's API
                  spec fields.
                properties:
                  encrypt:
                    description: EncryptPolicy configuration.
                    properties:
                      condition:
                        description: |-
                          A string containing the boolean expression that determines whether the
                          policy is applied.
                        maxLength: 1000
                        type: string
                      config:
                        description: The configuration of the policy.
                        properties:
                          encryptionKey:
                            description: The key to use for encryption.
                            properties:
                              aws:
                                description: AWS configuration.
                                properties:
                                  arn:
                                    description: The AWS KMS key ARN.
                                    maxLength: 2048
                                    minLength: 10
                                    pattern: ^arn:aws:kms:.+
                                    type: string
                                required:
                                - arn
                                type: object
                              static:
                                description: Static configuration.
                                properties:
                                  key:
                                    description: A static encryption key reference,
                                      either by ID or by value.
                                    maxProperties: 1
                                    minProperties: 1
                                    properties:
                                      id:
                                        type: string
                                      name:
                                        type: string
                                    type: object
                                required:
                                - key
                                type: object
                              type:
                                description: Type designates the type of configuration.
                                enum:
                                - aws
                                - static
                                minLength: 1
                                type: string
                            required:
                            - type
                            type: object
                            x-kubernetes-validations:
                            - message: aws must be set only when type is aws
                              rule: 'self.type == ''aws'' ? has(self.aws) : !has(self.aws)'
                            - message: static must be set only when type is static
                              rule: 'self.type == ''static'' ? has(self.static) :
                                !has(self.static)'
                          failureMode:
                            description: |-
                              Describes how to handle failing encryption or decryption.
                              Use `error` if the record should be rejected if encryption or decryption
                              fails.
                              Use `passthrough` to ignore encryption or decryption failure and continue
                              proxying the record.
                            enum:
                            - error
                            - passthrough
                            maxLength: 253
                            minLength: 1
                            type: string
                          partOfRecord:
                            description: Describes the parts of a record to encrypt.
                            items:
                              description: |-
                                EncryptionRecordPart * key - encrypt the record key
                                * value - encrypt the record value
                              type: string
                            type: array
                        required:
                        - encryptionKey
                        - failureMode
                        - partOfRecord
                        type: object
                      description:
                        description: A human-readable description of the policy.
                        maxLength: 512
                        type: string
                      enabled:
                        description: Whether the policy is enabled.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      labels:
                        additionalProperties:
                          description: LabelsValue is the value type for Labels.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                          type: string
                        description: |-
                          Labels store metadata of an entity that can be used for filtering an entity
                          list or for searching across entity types.

                          Keys must be of length 1-63 characters, and cannot start with "kong",
                          "konnect", "mesh", "kic", or "_".
                        type: object
                      name:
                        description: A unique user-defined name of the policy.
                        maxLength: 255
                        type: string
                    required:
                    - config
                    type: object
                  encryptFields:
                    description: ParsedRecordEncryptFieldsPolicyCreate configuration.
                    properties:
                      condition:
                        description: |-
                          A string containing the boolean expression that determines whether the
                          policy is applied.

                          When the policy is applied as a child policy of schema_validation, the
                          expression can also reference
                          `record.value` fields.
                        maxLength: 1000
                        type: string
                      config:
                        description: The configuration of the policy.
                        properties:
                          encryptFields:
                            description: Selects which fields to encrypt and with
                              what keys.
                            items:
                              description: |-
                                EventGatewayParsedRecordEncryptionSelector Selects fields of a parsed record
                                for encryption and defines what key to encrypt them with.
                              properties:
                                encryptionKey:
                                  description: The key to use for encryption.
                                  properties:
                                    aws:
                                      description: AWS configuration.
                                      properties:
                                        arn:
                                          description: The AWS KMS key ARN.
                                          maxLength: 2048
                                          minLength: 10
                                          pattern: ^arn:aws:kms:.+
                                          type: string
                                      required:
                                      - arn
                                      type: object
                                    static:
                                      description: Static configuration.
                                      properties:
                                        key:
                                          description: A static encryption key reference,
                                            either by ID or by value.
                                          maxProperties: 1
                                          minProperties: 1
                                          properties:
                                            id:
                                              type: string
                                            name:
                                              type: string
                                          type: object
                                      required:
                                      - key
                                      type: object
                                    type:
                                      description: Type designates the type of configuration.
                                      enum:
                                      - aws
                                      - static
                                      minLength: 1
                                      type: string
                                  required:
                                  - type
                                  type: object
                                paths:
                                  description: |-
                                    Selects which fields of the parsed record to encrypt.
                                    A maximum of 50 path entries are allowed.
                                  properties:
                                    type:
                                      description: Type designates the type of configuration.
                                      enum:
                                      - variant1
                                      - variant2
                                      minLength: 1
                                      type: string
                                    variant1:
                                      description: Variant1 configuration.
                                      items:
                                        x-kubernetes-preserve-unknown-fields: true
                                      type: array
                                    variant2:
                                      description: Variant2 configuration.
                                      type: string
                                  required:
                                  - type
                                  type: object
                              required:
                              - encryptionKey
                              - paths
                              type: object
                            type: array
                          failureMode:
                            description: |-
                              Describes how to handle a failure in a policy applied to produced records.
                              * `reject` - rejects the record batch.
                              * `passthrough` - passes the record silently to the backend cluster even
                              though policy execution failed.
                              * `mark` - passes the record to the backend cluster but marks it with a
                              `kong/policy-failure-<id>` header whose value is the reason for the policy
                              failure (truncated to 512 characters).
                            enum:
                            - reject
                            - passthrough
                            - mark
                            maxLength: 253
                            minLength: 1
                            type: string
                        required:
                        - encryptFields
                        - failureMode
                        type: object
                      description:
                        description: A human-readable description of the policy.
                        maxLength: 512
                        type: string
                      enabled:
                        description: Whether the policy is enabled.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      labels:
                        additionalProperties:
                          description: LabelsValue is the value type for Labels.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                          type: string
                        description: |-
                          Labels store metadata of an entity that can be used for filtering an entity
                          list or for searching across entity types.

                          Keys must be of length 1-63 characters, and cannot start with "kong",
                          "konnect", "mesh", "kic", or "_".
                        type: object
                      name:
                        description: A unique user-defined name of the policy.
                        maxLength: 255
                        type: string
                    required:
                    - config
                    type: object
                  modifyHeaders:
                    description: ModifyHeadersPolicyCreate configuration.
                    properties:
                      condition:
                        description: |-
                          A string containing the boolean expression that determines whether the
                          policy is applied.

                          When the policy is applied as a child policy of schema_validation, the
                          expression can also reference
                          `record.value` fields.
                        maxLength: 1000
                        type: string
                      config:
                        description: The configuration of the modify headers policy.
                        properties:
                          actions:
                            description: Actions are run in sequential order and act
                              on individual headers.
                            items:
                              description: |-
                                EventGatewayModifyHeaderAction represents a union type for EventGatewayModifyHeaderAction.
                                Only one of the fields should be set based on the Op.
                              properties:
                                op:
                                  description: Op designates the type of configuration.
                                  enum:
                                  - remove
                                  - set
                                  minLength: 1
                                  type: string
                                remove:
                                  description: Remove configuration.
                                  properties:
                                    key:
                                      description: The key of the header to remove.
                                      maxLength: 253
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  type: object
                                set:
                                  description: Set configuration.
                                  properties:
                                    key:
                                      description: The key of the header to set.
                                      maxLength: 253
                                      minLength: 1
                                      type: string
                                    value:
                                      description: The value of the header to set.
                                      maxLength: 253
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - value
                                  type: object
                              required:
                              - op
                              type: object
                            type: array
                        type: object
                      description:
                        description: A human-readable description of the policy.
                        maxLength: 512
                        type: string
                      enabled:
                        description: Whether the policy is enabled.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      labels:
                        additionalProperties:
                          description: LabelsValue is the value type for Labels.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                          type: string
                        description: |-
                          Labels store metadata of an entity that can be used for filtering an entity
                          list or for searching across entity types.

                          Keys must be of length 1-63 characters, and cannot start with "kong",
                          "konnect", "mesh", "kic", or "_".
                        type: object
                      name:
                        description: A unique user-defined name of the policy.
                        maxLength: 255
                        type: string
                    required:
                    - config
                    type: object
                  schemaValidation:
                    description: ProduceSchemaValidationPolicy configuration.
                    properties:
                      condition:
                        description: |-
                          A string containing the boolean expression that determines whether the
                          policy is applied.
                        maxLength: 1000
                        type: string
                      config:
                        description: The configuration of the policy.
                        properties:
                          confluentSchemaRegistry:
                            description: SchemaRegistry configuration.
                            properties:
                              keyValidationAction:
                                description: |-
                                  Defines a behavior when record key is not valid.
                                  * reject - rejects a batch for topic partition. Only available for produce.
                                  * mark - marks a record with kong/server header and client ID value

                                  to help to identify the clients violating schema.
                                enum:
                                - reject
                                - mark
                                maxLength: 253
                                type: string
                              schemaRegistry:
                                description: A reference to a schema Registry.
                                properties:
                                  id:
                                    description: ID configuration.
                                    type: object
                                  name:
                                    description: Name configuration.
                                    properties:
                                      name:
                                        description: The unique name of the schema
                                          registry.
                                        maxLength: 255
                                        minLength: 1
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  type:
                                    description: Type designates the type of configuration.
                                    enum:
                                    - id
                                    - name
                                    minLength: 1
                                    type: string
                                required:
                                - type
                                type: object
                              valueValidationAction:
                                description: |-
                                  Defines a behavior when record value is not valid.
                                  * reject - rejects a batch for topic partition. Only available for produce.
                                  * mark - marks a record with kong/server header and client ID value

                                  to help to identify the clients violating schema.
                                enum:
                                - reject
                                - mark
                                maxLength: 253
                                type: string
                            type: object
                          json:
                            description: JSON configuration.
                            properties:
                              keyValidationAction:
                                description: |-
                                  Defines a behavior when record key is not valid.
                                  * reject - rejects a batch for topic partition. Only available for produce.
                                  * mark - marks a record with kong/server header and client ID value

                                  to help to identify the clients violating schema.
                                enum:
                                - reject
                                - mark
                                maxLength: 253
                                type: string
                              schemaRegistry:
                                description: A reference to a schema Registry.
                                properties:
                                  id:
                                    description: ID configuration.
                                    type: object
                                  name:
                                    description: Name configuration.
                                    properties:
                                      name:
                                        description: The unique name of the schema
                                          registry.
                                        maxLength: 255
                                        minLength: 1
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  type:
                                    description: Type designates the type of configuration.
                                    enum:
                                    - id
                                    - name
                                    minLength: 1
                                    type: string
                                required:
                                - type
                                type: object
                              valueValidationAction:
                                description: |-
                                  Defines a behavior when record value is not valid.
                                  * reject - rejects a batch for topic partition. Only available for produce.
                                  * mark - marks a record with kong/server header and client ID value

                                  to help to identify the clients violating schema.
                                enum:
                                - reject
                                - mark
                                maxLength: 253
                                type: string
                            type: object
                          type:
                            description: Type designates the type of configuration.
                            enum:
                            - confluentSchemaRegistry
                            - json
                            minLength: 1
                            type: string
                        required:
                        - type
                        type: object
                      description:
                        description: A human-readable description of the policy.
                        maxLength: 512
                        type: string
                      enabled:
                        description: Whether the policy is enabled.
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                      labels:
                        additionalProperties:
                          description: LabelsValue is the value type for Labels.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9A-Z]{1}([a-z0-9A-Z-._]*[a-z0-9A-Z]+)?$
                          type: string
                        description: |-
                          Labels store metadata of an entity that can be used for filtering an entity
                          list or for searching across entity types.

                          Keys must be of length 1-63 characters, and cannot start with "kong",
                          "konnect", "mesh", "kic", or "_".
                        type: object
                      name:
                        description: A unique user-defined name of the policy.
                        maxLength: 255
                        type: string
                    required:
                    - config
                    type: object
                  type:
                    description: Type designates the type of configuration.
                    enum:
                    - encrypt
                    - encryptFields
                    - modifyHeaders
                    - schemaValidation
                    minLength: 1
                    type: string
                required:
                - type
                type: object
              eventGatewayVirtualClusterRef:
                description: EventGatewayVirtualClusterRef is the reference to the
                  parent EventGatewayVirtualCluster object.
                properties:
                  konnectID:
                    description: |-
                      KonnectID is the schema for the KonnectID type.
                      This field is required when the Type is konnectID.
                    maxLength: 36
                    type: string
                  namespacedRef:
                    description: |-
                      NamespacedRef is a reference to a KeySet entity inside the cluster.
                      This field is required when the Type is namespacedRef.
                    properties:
                      name:
                        description: Name is the name of the referred resource.
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referred resource.

                          For namespace-scoped resources if no Namespace is provided then the
                          namespace of the parent object MUST be used.

                          This field MUST not be set when referring to cluster-scoped resources.
                        maxLength: 253
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    description: |-
                      Type defines type of the object which is referenced. It can be one of:

                      - konnectID
                      - namespacedRef
                    enum:
                    - konnectID
                    - namespacedRef
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: when type is namespacedRef, namespacedRef must be set
                  rule: 'self.type == ''namespacedRef'' ? has(self.namespacedRef)
                    : true'
                - message: when type is namespacedRef, konnectID must not be set
                  rule: 'self.type == ''namespacedRef'' ? !has(self.konnectID) : true'
                - message: when type is konnectID, konnectID must be set
                  rule: 'self.type == ''konnectID'' ? has(self.konnectID) : true'
                - message: when type is konnectID, namespacedRef must not be set
                  rule: 'self.type == ''konnectID'' ? !has(self.namespacedRef) : true'
            required:
            - eventGatewayVirtualClusterRef
            type: object
          status:
            description: EventGatewayVirtualClusterProducePolicyStatus defines the
              observed state of EventGatewayVirtualClusterProducePolicy.
            properties:
              conditions:
                description: Conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              gatewayID:
                description: GatewayID is the Konnect ID of the parent Gateway.
                properties:
                  id:
                    description: ID is the unique identifier of the Konnect entity
                      as assigned by Konnect API.
                    maxLength: 256
                    type: string
                type: object
              id:
                description: |-
                  ID is the unique identifier of the Konnect entity as assigned by Konnect API.
                  If it's unset (empty string), it means the Konnect entity hasn't been created yet.
                maxLength: 256
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              organizationID:
                description: OrgID is ID of Konnect Org that this entity has been
                  created in.
                maxLength: 256
                type: string
              serverURL:
                description: ServerURL is the URL of the Konnect server in which the
                  entity exists.
                maxLength: 512
                type: string
              virtualClusterID:
                description: VirtualClusterID is the Konnect ID of the parent VirtualCluster.
                properties:
                  id:
                    description: ID is the unique identifier of the Konnect entity
                      as assigned by Konnect API.
                    maxLength: 256
                    type: string
                type: object
            type: object
        required:
        - metadata
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
